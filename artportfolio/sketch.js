function setup() {
    //apply picture to entire screen
    //createCanvas(windowWidth, windowHeight);
}

function draw() {
    //background(255); 
    //have a cool cursor? lol 
    //noFill();
    fill(245, 189, 255);
    stroke(245, 189, 255); 
    ellipse(mouseX, mouseY, 5); 
    
}